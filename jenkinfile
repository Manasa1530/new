pipeline {
  agent any
      stages{
          stage("build"){
           steps{
               echo 'building file'
               }
            }
          stage("testing"){
           steps{
             echo 'testing file'
              }
           }   
          stage("deploy"){
            steps{
             echo 'deploying file'
               }
           }   
        }   
      }    
